# 核心依赖
# 兼容 Windows XP: PyInstaller 4.x 引导程序支持 XP（5.x/6.x 引导程序要求 Win7+）
pyinstaller>=4.0,<5.0

# 文档解析（以下版本均兼容 Python 3.8）
python-docx>=1.0.0
olefile>=0.47
openpyxl>=3.0.0
xlrd>=2.0.0
python-pptx>=0.6.21
pdfplumber>=0.7.0
pypdf>=3.0.0

# 加密压缩
pyzipper>=0.3.6
